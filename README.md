# docker-python-buildenv

An environment suited to build Python projects. Useful within CI environments,
e.g. GitLab CI.

It does not include a boatload of stuff, just the things I need. Notably `poetry`.
